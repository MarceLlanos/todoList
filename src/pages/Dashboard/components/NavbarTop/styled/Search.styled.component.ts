// import styled from 'styled-components';

import styled from 'styled-components';

export const Search = styled.div`
	position: static;
	margin-right: 25px;
	margin-left: 0;
	width: 536px;
	&:hover {
		backgroundcolor: var(--grey200);
	}
`;
